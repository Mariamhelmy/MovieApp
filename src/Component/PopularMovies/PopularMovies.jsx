import React from 'react'
import { Link } from 'react-router-dom';


// export default function PopularMovies() {
//   return (
    // <div>
      
    {/* <div className="coll d-flex justify-content-center mt-3">
                      <div className={`${styles.vonta}  bg-light text-dark`}>
                             <ul>
    
                                    <li>hh</li>
                                   <hr className='w-100'/>
                                   <li>nn</li>
                                     <hr />
                                  <li>cc</li>
                                    <hr />
                                 <li>ss</li>

                             </ul>
                     </div>
              </div>  */}
              {/* <div className={`${styles.hook}`}>
        <div className={`${styles.hoka}`}>
            
            <div className={`${styles.search}`}>
               <div className={styles.divo}>
                  <form className={ `${styles.size} d-flex`} role="search">
                      <input className="form-control me-2" type="search" placeholder="Search" aria-label="Search" onChange={(e) => setSearchQuery(e.target.value)}/>
                      <button className="btn btn-outline-danger" type="submit">Search</button>
                 </form>
           
                </div>
        
            </div> 
           
        <div> */}
        {/* {movies.length > 0 ? (
          <ul>
            {movies.map((movie) => (
              <li key={movie.id}>
                <h3>{movie.title}</h3>
                <img
                  src={`https://image.tmdb.org/t/p/w200${movie.poster_path}`}
                  alt={movie.title}
                  style={{ width: '200px', borderRadius: '10px' }}
                />
              </li> */}
            {/* ))}
          </ul>
        ) : (
          <p>No movies found</p>
        )}
      
    </div>
 
</div>


  )
// } */}
